-- Archivo de configuracion

en este archivo encontraras los parametros para poder ejecutar este repo de git

instalaciones necesarias:
python 3.10 + o superior
node 20
mongo DB
Docker -> se utilizo un contenedor con mongo para poder guardar registro dentro de la bd. Te dejo el archivo docker compose para que puedas utilizarlo.

extra:
para poder ejecutar el archivo docker debes tener instalado docker y docker compose en tu pc, si eres usuario windows puedes instalr docker.

sobre el proyecto:
es un pequeño proyecto que trata sobre una api sencilla construida mediante back y front esto con python fastapi y react para el front tambien utiliza axios para poder
conectar y traer los datos desde el back hacia el front
